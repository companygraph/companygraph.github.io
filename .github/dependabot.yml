# Most npm dependencies here are devDependencies — playwright and the export scripts drive
# `npm run verify`, `npm run og` and `npm run pdf`, and none of that reaches a visitor.
#
# d3 is the exception, and it is the reason this file matters more here than on the sibling
# sites. `example/index.html` loads `example/d3.v7.min.js`, a vendored copy of the pinned
# package's build — so d3 is the one dependency in this repository that a visitor executes.
# `verify/instance.test.mjs` asserts the two are byte-identical, which means a Dependabot
# bump of d3 lands red on purpose, with the message "copy it again". That is the correct
# signal, not a nuisance to be silenced with an ignore rule: an ignored d3 is a shipped
# asset that ages forever with nothing saying so. Clearing such a pull request is
# re-vendoring the file and re-rendering the card that names it — `npm run og`.
#
# The shape is copied from guestgraph/engine, deliberately — the same duplication the design
# tokens and the deck footer have, and the same absence of a tripwire. Grouping minor and
# patch means a quiet week is one pull request, not five; a major arrives on its own.
version: 2
updates:
  # Two package.json files, and only the root one is exercised by CI: ci.yml runs a single
  # `npm ci` at the root before `npm run verify`. A bump under talks/intro therefore gets a
  # green run that proves nothing about the directory it changed — the deck's PDF export is
  # what uses those packages, and `npm run pdf` is not a CI step because it writes files this
  # repository commits. Read those pull requests; do not merge them on the checkmark.
  - package-ecosystem: "npm"
    directories:
      - "/"
      - "/talks/intro"
    schedule:
      interval: "weekly"
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"
